use std::collections::HashSet;

use crate::types::*;

//fn process(model: &VariantModel, input: &str, max_anagram_distance: u8, max_edit_distance: u8, max_matches: usize, score_threshold: f64, stop_criterion: StopCriterion, output_lexmatch: bool, json: bool, seqnr: usize, cache: &mut Option<Cache>) {
//}
